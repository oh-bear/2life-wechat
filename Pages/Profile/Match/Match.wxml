<view class="container">
  <view class="title">
    <text>{{title}}</text>
    <!-- <text class="cancel">{{cancel}}</text> -->
  </view>
  <!-- <view class="choice">
     <view class="type">
      <view class="random">
        <text>随机匹配</text>
      </view>
      <view class="accurate sel">
        <text>ID 匹配</text>
      </view>
    </view>  -->

    <!-- ID  -->
    <!-- <view class="id">
      <view>请输入对方的ID</view>
      <input placeholder="Example: 123456" />
    </view> -->

    <!-- random  -->
    <!-- <view class="options">
      <view class="item" wx:for="{{matchItems}}" wx:for-item="matchItem" wx:for-index="matchIndex" wx:key="{{matchIndex}}">
        <view class="item-title">
          <text>{{matchItem.title}}</text>
        </view>
        <view class="option">
          <view wx:for="{{matchItem.items}}" wx:for-index="itemIndex" wx:key="{{item.value}}" bindtap="chooseMatchType" data-value="{{item.value}}" data-index="{{matchIndex}}" class="{{choose[matchIndex] === item.value ? 'sel' : ''}}">
            {{item.title}}
          </view>
        </view>
      </view>
    </view>
  </view>   -->

  <!-- matching  -->
  <!-- <view class="matching">
    <view class="third">
      <view class="second">
        <view class="first"></view>
      </view>
    </view>
  </view> -->

  <!-- success -->
  <!-- <view class="match-success">
    <view class="third">
      <view class="second">
        <view class="first">
          <image class="image" src="../Images/profile.png" />
        </view>
      </view>
    </view>
  </view> -->

  <!-- fail  -->
  <view class="match-fail">

  </view>

  <!-- <view class="name">
    <view>
      <text>{{name}}</text>
    </view>
    <view class="nick">
      <text>{{nickname}}</text>
    </view>
  </view> -->
  <!-- <view class="match-btn">
    开始匹配
  </view> -->
</view>

<!-- <view class="mask">
  <swiper class="swiper" bindchange="swiperChange">
    <block wx:for="{{tipsList}}">
      <swiper-item>
        <image class="image" src="{{item}}" />
      </swiper-item>
    </block>
  </swiper>
  <view class="btn" wx:if="{{currentSwiperItem === 2}}">
    <button>我明白了</button>
  </view>
</view> -->